import { EntityWorkspace } from './EntityWorkspace'

import styles from './ChapterWorkspace.module.css'

export const ChapterWorkspace = () => {
    return <EntityWorkspace type='chapter'>{chapter => <>

    </>}</EntityWorkspace>
}
