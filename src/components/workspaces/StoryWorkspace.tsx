import { EntityWorkspace } from './EntityWorkspace'
import styles from './StoryWorkspace.module.css'

export const StoryWorkspace = () => {
    return <EntityWorkspace type='story'>{story => <>

    </>}</EntityWorkspace>
}
