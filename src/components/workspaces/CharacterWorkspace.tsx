import { EntityWorkspace } from './EntityWorkspace'
import styles from './CharacterWorkspace.module.css'

export const CharacterWorkspace = () => {
    return <EntityWorkspace type='character'>{character => <>

    </>}</EntityWorkspace>
}
